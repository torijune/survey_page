{
  "name": "survey-machine",
  "version": "1.0.0",
  "description": "설문조사 생성, 배포, 응답 수집 시스템",
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm start",
    "backend": "cd backend && source venv/bin/activate && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "backend:stop": "pkill -f 'uvicorn app.main:app' || true",
    "deploy:frontend": "cd app && bash deploy-frontend.sh",
    "deploy:backend": "cd backend && bash deploy-lambda.sh"
  },
  "keywords": ["survey", "설문조사"],
  "author": "",
  "license": "ISC"
}
